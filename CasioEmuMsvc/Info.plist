<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>CasioEmu</string>

    <key>CFBundleDisplayName</key>
    <string>CasioEmu</string>

    <key>CFBundleExecutable</key>
    <string>CasioEmuApp</string>

    <key>CFBundleIdentifier</key>
    <string>com.casioemu.app</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <key>CFBundleShortVersionString</key>
    <string>1.0</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- Bắt buộc để iOS nhận ra đây là app iPhone -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Bắt buộc cho UIKit - tên class AppDelegate -->
    <key>NSPrincipalClass</key>
    <string>UIApplication</string>

    <!-- Bắt buộc: ít nhất phải có một orientation -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Tắt launch storyboard (dùng code-only UI) -->
    <key>UILaunchStoryboardName</key>
    <string></string>

    <!-- Yêu cầu thiết bị tối thiểu -->
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>

    <!-- Tắt status bar để SDL có toàn màn hình -->
    <key>UIStatusBarHidden</key>
    <true/>

    <!-- Cho phép SDL render full screen -->
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>

    <!-- Mô tả truy cập Files (cần để đọc ROM từ Documents) -->
    <key>UIFileSharingEnabled</key>
    <true/>

    <key>LSSupportsOpeningDocumentsInPlace</key>
    <true/>

    <!-- Cho phép truy cập local network (nếu cần debug) -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Used for debug purposes only.</string>
</dict>
</plist>
